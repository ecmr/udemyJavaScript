// Com ?:

const pontos = 99;
const nivel = pontos >= 100 ? 'Pontos são bons' : 'Pontos sÃ£o baixos';

console.log(nivel);

// default;

const corUser = 'blue';
const corPadrao = corUser || 'Pink';
console.log(corPadrao);
